apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-add-task-api-ingress
spec:
  ingressClassName: ingress-nginx-controller
  rules:
    - host: add-tasks-api.applicationtesting.online
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-add-task-api
                port:
                  number: 80
